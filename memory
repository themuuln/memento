#!/usr/bin/env python3
"""memory — unified memory management CLI.
    
Install: ln -sf ~/.agent-memory/memory ~/.local/bin/memory
         (or add ~/.agent-memory to PATH)
"""

import sys
import os

# Ensure the package is importable from ~/.agent-memory
_pkg_dir = os.path.dirname(os.path.abspath(__file__))
if _pkg_dir not in sys.path:
    sys.path.insert(0, os.path.dirname(_pkg_dir))

from memory_cli.cli import main

sys.exit(main())
